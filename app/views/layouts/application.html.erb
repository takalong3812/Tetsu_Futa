<!DOCTYPE html>
<html>
  <head>
    <title>Tetsu_Futa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
         <a class="navbar-brand p-3" href="/"><%= image_tag 'manhole.jpg', :size =>'150x150' %></a>
         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
         </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">


        <% if user_signed_in? %>
              <p>ようこそ<%= current_user.name %>さん！</p>
            <% end %>
    <ul class="navbar-nav ml-auto">
       <% if user_signed_in? %>
       <li class="navbar-link"><%= link_to "マイページ", user_user_path(current_user), class: "btn btn-outline-dark" %></li>
       <li class="navbar-link"><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-dark" %></li>
       <% elsif admin_signed_in? %>
       <li class="navbar-link"><%= link_to "ホーム", admin_root_path, class: "btn btn-outline-dark" %></li>
       <li class="navbar-link"><%= link_to "ユーザー覧", admin_users_path, class: "btn btn-outline-dark" %></li>
       <li class="navbar-link"><%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "btn btn-outline-dark" %></li>

      <% else %>

      <li class="nav-item">
        <%= link_to new_user_session_path, class:'nav-link text-dark' do %>
         <i class="fa-solid fa-right-to-bracket"></i>ログイン
        <% end %>

        <%= link_to root_path, class:'nav-link text-dark' do %>
        <i class="fa-solid fa-house"></i>ホーム
        <% end %>

      <% end %>
    </ul>
     </div>
    </div>
   </nav>
   </header>
   <main class="mb-auto">
    <%= yield %>
    </main>

   <footer class="bg-dark text-white">
      <div class="container">
        <p>©鉄　蓋</p>
      </div>
    </footer>
  </body>
</html>
